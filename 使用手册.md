# 排水系统智能调度平台使用手册

本文档面向课程设计、毕业设计演示和日常项目交接，说明本项目的安装、初始化、页面功能、后台操作和常见问题。

## 1. 项目简介

本系统是一个基于 Flask 的排水系统智能调度平台，集成了以下能力：

- 首页总览：展示当前降雨、水位、能耗、风险和调度建议
- 数据监控大屏：展示趋势图、风险仪表、策略对比和事件流
- 调度决策解释：展示调度建议、命中规则、置信度和反事实推演
- 动态仿真：支持强化学习、规则调度和手动控制三种模式
- 后台管理中心：支持数据筛选、统计分析、记录管理和用户管理

## 2. 运行环境

建议环境如下：

- Python 3.10 及以上
- MySQL 8.0 及以上
- macOS / Linux / Windows（建议使用类 Unix 终端）

项目根目录关键文件如下：

- `app.py`：后端入口
- `run.sh`：一键启动脚本
- `requirements.txt`：Python 依赖
- `.env.example`：环境变量模板
- `templates/`：页面模板
- `static/js/`、`static/css/`：前端脚本和样式
- `models/`：强化学习模型文件
- `data/`：仿真与处理后的数据

## 3. 安装步骤

### 3.1 创建虚拟环境并安装依赖

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 3.2 创建数据库

本项目会自动创建表，但不会自动创建数据库本身。请先在 MySQL 中创建数据库：

```sql
CREATE DATABASE drainage CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 3.3 配置环境变量

复制模板：

```bash
cp .env.example .env
```

然后修改 `.env` 中的数据库连接信息：

```env
FLASK_SECRET_KEY=replace-with-a-random-string
FLASK_DEBUG=false

MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=your_mysql_user
MYSQL_PASSWORD=your_mysql_password
MYSQL_DB=drainage
MYSQL_CHARSET=utf8mb4

ALLOW_PUBLIC_REGISTER=false
# ADMIN_INIT_TOKEN=replace-with-init-token
```

参数说明：

- `FLASK_SECRET_KEY`：Flask 会话密钥，必须设置
- `FLASK_DEBUG`：是否开启调试模式，答辩或演示建议为 `false`
- `MYSQL_*`：MySQL 连接参数
- `ALLOW_PUBLIC_REGISTER`：是否允许公开注册
- `ADMIN_INIT_TOKEN`：可选，若设置则初始化管理员时必须提供令牌

## 4. 启动系统

### 4.1 推荐方式：一键启动

```bash
./run.sh
```

脚本会自动：

- 检查 `.venv` 是否存在
- 检查 `.env` 是否存在
- 激活虚拟环境
- 加载环境变量
- 运行 `python app.py`

### 4.2 手动启动

```bash
source .venv/bin/activate
set -a
source .env
set +a
python app.py
```

启动成功后，默认访问地址为：

`http://127.0.0.1:5000`

## 5. 首次部署与初始化

### 5.1 首次登录前需要做什么

首次启动后，数据库中可能还没有管理员账号。此时请打开：

`http://127.0.0.1:5000/admin/login`

点击登录页中的“初始化系统”按钮。

系统会自动完成以下操作：

- 创建 `admin_users` 用户表
- 创建 `inventory_data` 业务数据表
- 创建默认管理员账号

默认管理员账号为：

- 用户名：`admin`
- 密码：`admin123456`

初始化成功后，请立即登录并修改或重建管理员账号，避免继续使用默认密码。

### 5.2 初始化令牌说明

如果环境变量中设置了 `ADMIN_INIT_TOKEN`，则初始化时必须同时提供该令牌，否则会被拒绝。

适用场景：

- 部署到共享服务器
- 答辩环境不希望任意人初始化后台
- 演示时需要保护管理员入口

## 6. 登录与权限说明

系统有两类角色：

- `admin`：管理员
- `user`：普通用户

权限差异如下：

- 管理员可以查看后台、增删改记录、管理用户
- 普通用户可以登录后台、查看数据、执行查询和统计，但不能删除或新增用户，也不能增删改业务记录

### 6.1 注册机制

是否允许公开注册由 `ALLOW_PUBLIC_REGISTER` 控制：

- `false`：注册页关闭，只能由管理员创建账号
- `true`：开放注册，注册的新账号默认角色为 `user`

## 7. 页面功能说明

## 7.1 首页总览

首页是系统统一入口，展示实时概览信息，包括：

- 当前降雨
- 平均水位
- 总能耗
- 风险等级与风险分数
- 系统评分
- 平均奖励
- 节点与蓄水压力
- 泵站运行状态
- 调度建议和系统解释

首页数据来源于 `/api/data`，前端每 3 秒自动刷新一次。

页面底部提供四个入口按钮：

- 数据监控大屏
- 调度决策解释
- 进入动态仿真
- 后台管理中心

## 7.2 数据监控大屏

监控大屏适合答辩展示和运行态观察，主要内容包括：

- 降雨强度趋势
- 节点水位与安全阈值
- 能耗负荷
- 规则策略与强化学习策略对比
- 综合风险仪表
- 节点水位热力图
- 泵站开关占比
- 系统健康雷达图
- 运行事件流

使用方式：

- 点击“立即刷新”可手动更新
- 页面默认每 3 秒自动刷新
- 页脚会显示最近更新时间和 Step 编号

适合展示内容：

- 当前系统是否存在高风险
- RL 策略与规则策略的差异
- 不同节点在时间上的水位变化

## 7.3 调度决策解释页

该页面用于展示一条“当前时刻”的调度建议，并解释其依据。页面内容包括：

- 当前系统状态
- 调度动作
- 决策时间
- 策略版本
- 决策 ID
- 解释签名
- 规则命中情况
- 置信度与不确定性
- 阈值敏感性分析
- 反事实推演

特别说明：

- 本页面当前由 `/api/decision` 生成演示型数据
- 水位、降雨等数值会随机变化
- 页面每 3 秒自动刷新一次

因此，这个页面更适合用来展示“可解释决策”的设计思路，而不是严格复现仿真历史。

## 7.4 动态仿真页

动态仿真页是本项目的核心演示页面，支持三种模式：

- 强化学习
- 规则调度
- 手动模式

### 7.4.1 页面主要区域

- 顶部控制区：启动仿真、暂停、重置、返回首页
- 模式切换区：RL / Rule / Manual
- 泵站状态区：显示 3 个泵站的开启或关闭状态
- 蓄水池动态区：显示 3 个蓄水池的水量变化
- 实时状态区：显示模型、场景、策略、风险、水位、流量、能耗、溢流、奖励
- 图表区：显示状态与奖励轨迹、规则与 RL 的对比
- 回放区：通过滑块查看历史步骤

### 7.4.2 基本操作流程

1. 打开仿真页面
2. 选择一种模式
3. 点击“启动仿真”
4. 观察泵站、水位、奖励和对比图变化
5. 需要暂停时点击“暂停”
6. 需要重新开始时点击“重置”

### 7.4.3 三种模式说明

强化学习模式：

- 使用 `models/` 目录中的训练模型进行动作决策
- 若模型不可用，系统可能回退到规则策略

规则调度模式：

- 使用后端规则逻辑进行调度
- 适合与 RL 模式做对比

手动模式：

- 用户可手动控制 3 个泵站的开关
- 可点击单个泵站按钮，也可使用“全部开启”“全部关闭”

### 7.4.4 仿真图表说明

状态与奖励轨迹图：

- 横轴为时间步
- 纵向展示降雨、奖励、溢流等变化

规则 vs 强化学习图：

- 对比总溢流
- 对比总能耗
- 对比平均奖励

一般情况下：

- 总溢流越低越好
- 总能耗需要尽量合理
- 平均奖励越高越好

## 7.5 后台管理中心

后台管理中心主要用于数据查询、统计分析和权限管理。

### 7.5.1 查询筛选

支持的筛选条件包括：

- 风险等级
- 关键词
- 开始时间
- 结束时间
- 最小/最大降雨
- 最小/最大入流
- 最小/最大溢流
- 最小/最大水位

建议用法：

- 先按时间范围筛选
- 再按风险等级缩小数据范围
- 如需查某条记录，可通过关键词搜索时间或 ID

### 7.5.2 统计概览

后台会展示以下统计指标：

- 总记录数
- 平均降雨
- 平均水位
- 峰值水位
- 平均入流
- 总溢流

并提供以下统计图：

- 时间序列图
- 风险分布图
- 入流分布图

### 7.5.3 记录管理

后台记录表支持：

- 新增记录
- 编辑记录
- 删除记录
- 分页浏览

主要字段包括：

- 时间
- 降雨
- 入流
- 节点 1/2/3 水位
- 节点 1/2/3 流量
- 节点 1/2/3 蓄水

说明：

- 风险等级可手动指定，也可由系统自动推导
- 当前后台展示基于 `inventory_data` 表
- `overflow`、`risk_level`、平均水位等部分展示值是系统根据水位规则计算得到的

### 7.5.4 用户管理

用户管理模块仅管理员可见，可执行：

- 新增用户
- 修改用户角色
- 启用或禁用用户
- 删除用户

普通用户登录后不会看到该模块。

## 8. 数据表说明

## 8.1 `admin_users`

系统自动创建，用于存储后台账号：

- `id`
- `username`
- `password_hash`
- `role`
- `is_active`
- `created_at`
- `updated_at`

## 8.2 `inventory_data`

系统自动创建，用于存储后台管理和统计使用的数据：

- `id`
- `time`
- `rain`
- `water_1`
- `water_2`
- `water_3`
- `flow_1`
- `flow_2`
- `flow_3`
- `storage_1`
- `storage_2`
- `storage_3`
- `inflow`
- `created_at`

说明：

- 后台接口会把 `time` 映射为页面中的“时间”或 `record_time`
- 后台接口会把 `water_1` 映射为 `water_node1` 这类前端字段
- `overflow` 和 `risk_level` 不是表内原始字段，而是后端根据水位推导

## 9. 推荐演示流程

如果用于答辩或成果演示，建议按以下顺序展示：

1. 进入登录页，说明系统支持管理员初始化与权限控制
2. 登录后台，展示查询筛选、统计概览和记录管理
3. 新增或编辑一条记录，说明系统支持基础数据治理
4. 打开监控大屏，展示趋势图、风险仪表和事件流
5. 打开调度决策解释页，说明系统具备“为什么这么调度”的解释能力
6. 打开动态仿真页，切换 RL、规则、手动三种模式进行对比
7. 重点说明 RL 在溢流控制、能耗和奖励上的综合优势

## 10. 常见问题

### 10.1 启动时报数据库连接失败

请检查：

- MySQL 服务是否已经启动
- `.env` 中的主机、端口、用户名、密码是否正确
- 数据库 `drainage` 是否已经创建

### 10.2 登录页点击初始化系统无反应或失败

请检查：

- 数据库是否可连接
- 是否设置了 `ADMIN_INIT_TOKEN`
- 如果设置了初始化令牌，前端调用时是否提供了正确令牌

### 10.3 打开后台后没有数据

可能原因：

- `inventory_data` 表为空
- 查询条件过严，筛选后没有命中结果

可先清空筛选条件，再重新查询。

### 10.4 无法注册新用户

请检查环境变量：

```env
ALLOW_PUBLIC_REGISTER=true
```

如果该值为 `false`，则只能由管理员在后台创建用户。

### 10.5 决策解释页每次内容都不同

这是当前设计使然。该页面使用演示型随机数据来展示解释逻辑，因此会定时刷新并产生不同的决策结果。

### 10.6 强化学习模式没有明显效果

请检查：

- `models/` 下模型文件是否存在
- 模型是否与当前环境兼容
- 是否已经成功加载 RL 模型，若加载失败系统可能回退到规则策略

## 11. 维护建议

- 首次初始化后立即修改默认管理员密码
- 正式演示前关闭 `FLASK_DEBUG`
- 正式演示前关闭公开注册
- 对 `inventory_data` 做定期备份
- 若用于论文答辩，建议提前准备一批稳定演示数据

## 12. 测试建议

基础测试命令：

```bash
source .venv/bin/activate
pytest -q
```

建议在以下场景下手工回归：

- 管理员初始化
- 登录/退出登录
- 记录新增、编辑、删除
- 用户角色切换
- 仿真模式切换
- 首页和大屏自动刷新

## 13. 总结

本系统已经具备一个完整演示平台所需的主要能力：

- 有后台账号与权限体系
- 有基础业务数据管理能力
- 有监控可视化能力
- 有决策解释能力
- 有强化学习与规则调度对比能力

对于课程设计、毕业设计和答辩展示而言，这份项目已经可以形成“数据管理 + 智能调度 + 可解释展示 + 动态仿真”的完整闭环。
